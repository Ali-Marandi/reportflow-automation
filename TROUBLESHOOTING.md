# ReportFlow Troubleshooting Guide 🛠️

این سند به شما کمک می‌کند تا مشکلات رایج در هنگام استفاده از ابزار ReportFlow را شناسایی و رفع کنید.

## 1. مشکلات مربوط به منابع داده (Data Source Issues)

### خطا: `FileNotFoundError`
*   **علت:** مسیر فایل در `config.json` اشتباه است یا فایل وجود ندارد.
*   **راه حل:** بررسی کنید که آیا مسیر فایل نسبت به فایل کانفیگ درست است. از ابزار `manage_data_sources.py` برای اصلاح مسیر استفاده کنید.

### خطا: `RuntimeError: Failed to retrieve URL`
*   **علت:** مشکلات شبکه، تایم‌اوت یا مسدود شدن توسط سرور مقصد.
*   **راه حل:** 
    - مقدار `timeout` را در کانفیگ افزایش دهید.
    - تعداد `retries` را بیشتر کنید.
    - مطمئن شوید که سرور مقصد نیاز به Authentication خاصی ندارد (نسخه فعلی از Basic Auth پشتیبانی نمی‌کند).

## 2. مشکلات پردازش داده (Processing Issues)

### خطا: `ValueError: could not convert string to float`
*   **علت:** داده‌های عددی حاوی کاراکترهای غیرمجاز هستند که سیستم نرمال‌سازی قادر به پاکسازی آن‌ها نبوده است.
*   **راه حل:** داده‌های ورودی را بررسی کنید. سیستم به طور خودکار کاما (`,`) و فضاهای خالی را حذف می‌کند، اما کاراکترهای خاص دیگر ممکن است نیاز به پیش‌پردازش داشته باشند.

## 3. مشکلات خروجی و قالب (Output Issues)

### خطا: `TemplateNotFound`
*   **علت:** پوشه `templates` یا `custom_templates` در مسیر مورد نظر یافت نشد.
*   **راه حل:** مطمئن شوید که اسکریپت `customize_template.py` را اجرا کرده‌اید و دایرکتوری ایجاد شده در کنار اسکریپت اصلی شما قرار دارد.

## 4. مشکلات استقرار و زمان‌بندی (Deployment & Scheduling)

### خطا: `ModuleNotFoundError: No module named 'schedule'`
*   **علت:** کتابخانه زمان‌بندی نصب نشده است.
*   **راه حل:** دستور `pip install schedule` را اجرا کنید. در Docker، این مورد به طور خودکار در فایل `Dockerfile` گنجانده شده است.

---
برای گزارش باگ‌های پیچیده‌تر، لطفاً یک **Issue** در ریپازیتوری گیتهاب ثبت کنید.
